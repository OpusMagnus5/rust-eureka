#[derive(Debug)]
enum CardSuit {
    Hearts,
    Diamonds,
    Spades,
    Clubs,
}

fn intro_to_enums() {
    let first_card = CardSuit::Hearts; // inicjalizacja enuma
}

// ============================================================================================== //

fn main() {
    
}
